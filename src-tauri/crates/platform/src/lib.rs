//! Host operating-system capabilities.
//!
//! This crate is the home for process, filesystem, registry, clipboard, log
//! watcher, and other platform adapters. It should keep OS detail out of
//! `runtime` and keep pure parsing/model code in `domain`.
